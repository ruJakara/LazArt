[project]
name = "prsbot"
version = "2.0.0"
description = "Telegram bot for Russian infrastructure news monitoring"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
